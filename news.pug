extends ./layout/_layout-page.pug

block content

  section
    .container
      .section-banner-news
        .text
          h4 Stay up to date!
        //- text
      //- section-banner-contact
    //- container
  //- section
  
  section
    .section-news
      .container
        h4 Comments
        #thread.curia-thread Please wait...
      //- container
    //- section-news
  //- section
  
  section.join
    .container
      | If you are looking for an opportunity to challenge yourself and grow, then we could just be the company you are looking for.&nbsp;
      a.btn.btn-default.btn-action(href='') Join Us Now
    //- container
  //- section

block scripts
  // External Script goes here...
  script(src="http://ttsite.crowdhound.io/libs/mustache-2.1.3/mustache.min.js").
  script(src="http://ttsite.crowdhound.io/js/curia-0.1.2.js").
  script.
    // Initialize Crowdhound
    CrowdHound.init({
      apiVersion: '2.0',
      serverUrl: 'http://ttsite.crowdhound.io/api/healthcheck:80',
      tenantId: 'examples',
      debug: false,
      themes: {
        "my-theme": {
          // templates
          "wrapper"       : '/templates/comments/wrapper',
          "commentList_0" : '/templates/comments/commentList',
          "comment_1"     : '/templates/comments/comment',
          "*_0"           : '/templates/basic/hidden',
          // extra variables passed to the templates
          "options": 	{ flat: true, textonly: true, readonly: false },
        },
      }
    });

    // Display the comments for this tutorial
    var domSelector = '#thread';
    var elementId = '$example-text-comments';
    var theme = 'my-theme';
    CrowdHound.setAndCallLoader(domSelector, function() {
      CrowdHound.load(elementId, domSelector, theme);
    });
  //- script
    
